// The verification-stamp animation on the hero receipt is pure CSS
// (see .stamp-ring / .stamp-text in styles.css) so no JS is required for
// the current page. This file is a placeholder for future interactivity
// (e.g. a live testnet receipt lookup form).
